/** Default tax rate (decimal). Used as the initial value in settingsSlice and by mock data generators. Do NOT import this in UI components — use selectTaxRate from settingsSlice instead. */
export const TAX_RATE = 0.21;
